name: Update README

on:
  schedule:
    - cron: "* */12 * * *"
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    name: Update Profile README

    steps:

      - uses: Readme-Workflows/recent-activity@v2.4.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
